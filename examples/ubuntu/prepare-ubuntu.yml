---
# Example: Prepare Ubuntu/Debian nodes for Cozystack
# This playbook installs required packages, configures kernel parameters,
# and sets k3s server arguments compatible with k3s.orchestration collection.
#
# Works on both Ubuntu and Debian (same packages and apt module).
#
# Run before k3s deployment: ansible-playbook prepare-ubuntu.yml

- name: Prepare Ubuntu/Debian nodes for Cozystack
  hosts: cluster
  become: true
  vars:
    # Packages required by Cozystack on every node.
    # See README "Node Prerequisites" for what each is for.
    cozystack_packages:
      # Storage backends: NFS, iSCSI, Multipath
      - nfs-common
      - open-iscsi
      - multipath-tools
      # LINSTOR LVM/thin provisioning (required by Cozystack storage)
      - lvm2
      - thin-provisioning-tools
      # Kernel headers — Piraeus operator compiles DRBD 9.x at runtime
      - linux-headers-generic

    # Enable ZFS backend for LINSTOR. Installs zfsutils-linux and loads the
    # zfs kernel module. Ubuntu 22.04+ ships the ZFS kernel module in
    # linux-modules-extra-* (pulled in automatically).
    cozystack_enable_zfs: true

    # Enable KubeVirt virtualization. Loads vhost_net, tun, and kvm_intel or
    # kvm_amd kernel modules. QEMU and libvirt run inside KubeVirt pods, so
    # no host userspace packages are installed.
    cozystack_enable_kubevirt: true

    # Kernel modules required on every node.
    # - overlay, br_netfilter: containerd + standard Kubernetes networking
    # - openvswitch, geneve, ip_tables, iptable_nat: Kube-OVN
    cozystack_kernel_modules:
      - overlay
      - br_netfilter
      - openvswitch
      - geneve
      - ip_tables
      - iptable_nat

    # Kernel parameters for Cozystack
    cozystack_sysctl_params:
      - { name: fs.inotify.max_user_watches, value: "524288" }
      - { name: fs.inotify.max_user_instances, value: "8192" }
      - { name: fs.inotify.max_queued_events, value: "65536" }
      - { name: fs.file-max, value: "2097152" }
      - { name: fs.aio-max-nr, value: "1048576" }
      - { name: net.ipv4.ip_forward, value: "1" }
      - { name: net.ipv4.conf.all.forwarding, value: "1" }
      - { name: net.ipv6.conf.all.forwarding, value: "1" }
      - { name: net.bridge.bridge-nf-call-iptables, value: "1" }
      - { name: net.bridge.bridge-nf-call-ip6tables, value: "1" }
      - { name: vm.swappiness, value: "1" }

    # k3s server arguments required by Cozystack.
    # These disable built-in components that Cozystack replaces.
    cozystack_k3s_server_args: >-
      --disable=traefik
      --disable=servicelb
      --disable=local-storage
      --disable=metrics-server
      --disable-network-policy
      --disable-kube-proxy
      --flannel-backend=none
      --cluster-domain=cozy.local
      --kubelet-arg=max-pods=220

    # k3s server config YAML (CIDRs).
    cozystack_k3s_server_config_yaml: |
      cluster-cidr: 10.42.0.0/16
      service-cidr: 10.43.0.0/16

  handlers:
    - name: Restart multipathd
      ansible.builtin.systemd:
        name: multipathd
        state: restarted

  tasks:
    - name: Create k3s_cluster group for k3s.orchestration
      ansible.builtin.group_by:
        key: k3s_cluster

    - name: Set k3s server args for Cozystack
      ansible.builtin.set_fact:
        extra_server_args: >-
          {{ cozystack_k3s_server_args }}
          {{ cozystack_k3s_extra_args | default("") }}
      when: extra_server_args is not defined

    - name: Set k3s server config for Cozystack
      ansible.builtin.set_fact:
        server_config_yaml: "{{ cozystack_k3s_server_config_yaml }}"
      when: server_config_yaml is not defined

    - name: Install required packages
      ansible.builtin.apt:
        name: "{{ cozystack_packages }}"
        state: present
        update_cache: true

    - name: Load kernel modules at boot (containerd, Kubernetes, Kube-OVN)
      ansible.builtin.copy:
        dest: /etc/modules-load.d/cozystack.conf
        mode: "0644"
        content: "{{ cozystack_kernel_modules | join('\n') }}\n"

    - name: Load kernel modules now
      community.general.modprobe:
        name: "{{ item }}"
      loop: "{{ cozystack_kernel_modules }}"
      failed_when: false  # some may be built into the kernel

    - name: Ensure multipath drop-in directory exists
      ansible.builtin.file:
        path: /etc/multipath/conf.d
        state: directory
        mode: "0755"

    # CRITICAL: without this blacklist, multipathd grabs drbd* devices
    # and makes LINSTOR volumes inaccessible after reboot.
    - name: Blacklist DRBD devices in multipathd
      ansible.builtin.copy:
        dest: /etc/multipath/conf.d/cozystack-drbd-blacklist.conf
        mode: "0644"
        content: |
          blacklist {
              devnode "^drbd[0-9]+"
          }
      notify: Restart multipathd

    - name: Configure sysctl parameters
      ansible.posix.sysctl:
        name: "{{ item.name }}"
        value: "{{ item.value }}"
        sysctl_set: true
        state: present
        reload: true
      loop: "{{ cozystack_sysctl_params }}"

    - name: Stop and disable ufw
      ansible.builtin.systemd:
        name: ufw
        enabled: false
        state: stopped
      when: cozystack_flush_iptables | default(false)
      failed_when: false

    - name: Flush iptables INPUT chain
      ansible.builtin.iptables:
        chain: INPUT
        flush: true
      when: cozystack_flush_iptables | default(false)

    - name: Set iptables INPUT policy to ACCEPT
      ansible.builtin.iptables:
        chain: INPUT
        policy: ACCEPT
      when: cozystack_flush_iptables | default(false)

    - name: Enable iscsid service
      ansible.builtin.systemd:
        name: iscsid
        enabled: true
        state: started

    - name: Enable multipathd service
      ansible.builtin.systemd:
        name: multipathd
        enabled: true
        state: started

    # ZFS backend for LINSTOR (opt-out via cozystack_enable_zfs=false)
    - name: Install ZFS userspace tools
      ansible.builtin.apt:
        name: zfsutils-linux
        state: present
      when: cozystack_enable_zfs

    - name: Load ZFS kernel module
      community.general.modprobe:
        name: zfs
      when: cozystack_enable_zfs

    # KubeVirt kernel modules (opt-out via cozystack_enable_kubevirt=false).
    # QEMU and libvirt run inside KubeVirt pods; no host packages needed.
    - name: Load KubeVirt kernel modules at boot
      ansible.builtin.copy:
        dest: /etc/modules-load.d/cozystack-kubevirt.conf
        mode: "0644"
        content: |
          vhost_net
          tun
      when: cozystack_enable_kubevirt

    - name: Load KubeVirt kernel modules now
      community.general.modprobe:
        name: "{{ item }}"
      loop:
        - vhost_net
        - tun
        - kvm_intel
        - kvm_amd
      failed_when: false  # only one of kvm_intel/kvm_amd succeeds per CPU vendor
      when: cozystack_enable_kubevirt
